// src/lib/tasks/actions/index.ts
// Bootstrapper for pre-coded task action shortcuts.
// Currently a no-op; future versions can register common action templates here
// (e.g., "migrate SQL dialect", "create scheduled query") that bypass the
// full resolver flow.

export function setupTaskActions(): void {
  // No-op for now. Future action registrations go here.
}
